MY_ARRAY <- INPUT;
INITIAL N <- 1;

FOR i <- 0 TO i < ARRAY LENGTH -1 (EVERY ELEMENT OF ARRAY) DO:
    IF i != 6 DO:
        N++;
END FOR;
IF LAST ELEMENT OF ARRAY != 6 DO:
    PRINT N;
    ELSE DO:
        PRINT "-1" (SEQUENCE INVALID)